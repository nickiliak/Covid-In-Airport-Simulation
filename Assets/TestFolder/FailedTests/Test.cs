using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Grid grid = new(250, 150, 1f, new Vector3(0, 0, 0));
    }
    private void Update()
    {
    }
}
